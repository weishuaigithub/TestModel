# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestModel' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TestModel
   pod 'Model01', :path =>'Lib/Model01'
    
  target 'TestModelTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestModelUITests' do
    # Pods for testing
  end

end
